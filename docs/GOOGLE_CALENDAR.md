# Google Calendar Integration

- Use Google Calendar API to sync appointments.
- Patients receive calendar invites.
- Doctors see appointments in their calendar.
- Requires OAuth2 authentication and API key in `.env`.
